# Backend Configuration
# Port for the backend server (exposed on host)
# Set this to match your Caddy reverse proxy configuration
# Accessible at ws://localhost:${BACKEND_PORT}/ws/*
BACKEND_PORT=3000

# Frontend Configuration
# Port for the frontend (exposed on host, mapped to nginx port 80 inside container)
# Set this to match your Caddy reverse proxy configuration
# Accessible at http://localhost:${FRONTEND_PORT}
FRONTEND_PORT=5173

# Backend WebSocket URL for frontend
# In Docker, use the service name 'backend' instead of 'localhost'
# For production with SSL behind Caddy, use wss://your-domain.com
# For local development, use ws://localhost:3000
# NOTE: When using Caddy reverse proxy, set this to wss://your-domain.com
VITE_BACKEND_URL=ws://backend:3000
