# Backend Configuration
# Port for the backend server
BACKEND_PORT=3000

# Frontend Configuration
# Port for the frontend (mapped to nginx port 80 inside container)
FRONTEND_PORT=5173

# Backend WebSocket URL for frontend
# In Docker, use the service name 'backend' instead of 'localhost'
# For production with SSL, use wss://your-domain.com
# For local development, use ws://localhost:3000
VITE_BACKEND_URL=ws://backend:3000
